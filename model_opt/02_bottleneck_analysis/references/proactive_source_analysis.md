# 主动源码分析(Line A)

### 目标

从代码出发,用四维度思维框架主动发现优化机会——不依赖 profiling 异常信号。

很多高价值问题点（如同源 Linear 独立调用、KV cache 动态扩容、cross-attention 重复计算）在 profiling 中看不出异常（每个算子单独看都正常），只有理解代码结构才能识别。

### 分析节奏

第一轮做全面审视（覆盖下方所有分析对象和维度）。后续轮次不需要重新通读全部源码——结构性问题第一轮已发现,但**每次优化修改会改变计算结构和代码组织,需要重新审视**：

- **修改部分审视**：上次修改引入的新代码结构是否产生了新的问题或机会？如做了权重拆分后,拆分后的计算链是否产生了新的可合并点？做了函数封装后,是否有操作被留在了封装外可以合并进来？
- **计算语义深审**：不满足于"读一遍代码发现显而易见的冗余",而是对当前计算链逐操作追问——这段计算的数学表达能否等价重组？这组连续操作是否构成可融合 pattern？循环内的数据访问是否比必要的更复杂？这些需要深入理解计算语义,不是浏览代码能发现的。

### 穿透框架层

很多模型的推理路径被框架层层包裹,直接看入口代码看到的是框架调度逻辑而非模型计算。agent 必须穿透框架到达真实的模型实现代码。

**识别框架 wrapper**:看到 `model.generate()` / `trainer.predict()` / `pipeline()` 时,不要停在这里——这是框架入口,不是模型逻辑。

**定位真实 forward**:沿调用链向下,跳过 `Module.__call__` / hook dispatch / mixin 方法,找到项目自己的 `modeling_xxx.py` 中的 `forward()` 实现。

**区分两类代码**:
- 框架代码(site-packages 下):调度/hook/logging/兼容性处理——这里的开销属于 Line B 分析(dispatch overhead)
- 模型代码(项目目录下):真实的计算逻辑——这里的结构性冗余属于 Line A 分析

**常见框架穿透路径**:
- HuggingFace: `generate()` → `_generate_*()` → `model()` → `modeling_xxx.py:XxxModel.forward()`
- vLLM: `LLMEngine.step()` → `model_runner.execute_model()` → `model.forward()`
- 自定义脚本: `main()` → `model(input)` → 找到 `nn.Module` 子类的 `forward()`

#### 穿透层级量化

推理调用链从外到内分为多层，每层都可能贡献 host 开销。不要只分析 `forward()`——**每一层**都要量化。

1. 从入口到算子，列出调用链经过的所有层（如 `generate()` → `Module.__call__` → `forward()` → `F.linear` → `aten::matmul`）
2. 用 `parse_operator_details.py` 的 **Host Time by Call-Chain Layer**（inclusive Host Total，按调用链首个项目帧聚合）量化每层 host 开销占比——注意用 Total（含子调用）而非 Self，因为 wrapper/dispatch 层 Self≈0 但 Total 大
3. 记录每层占比，供确认节点 A 的优先级覆盖门禁使用（任何层 >10% total host time 须有候选）

> 注意：此步骤只做**量化记录**，不在此处提出方案。某层占比高意味着该层有优化空间，具体怎么改是 Phase 3 的工作。

#### 框架开销识别

穿透框架层不只是"识别"框架代码,还要判断框架 wrapper 的开销是否值得消除。当量化显示某层框架 wrapper 的 host time 占比 >5% 时,检查热路径中是否存在以下模式：

- 热路径中通过 `module(x)` 调用子模块,但该模块不需要 hooks/grad——调用链经过的 `__call__` + `__getattr__` 是否是不必要的开销？
- 热路径中反复访问 `self.xxx` 属性——每次 `__getattr__` 的 dict 搜索是否累积成显著开销？
- 热路径中使用 `nn.Linear` 等框架 API——中间经过的 `F.linear` 分支判断和权重转置是否可以跳过？
- 热路径中用 `List[Tensor]` 索引——索引操作是否触发了不必要的设备侧 op？

> 以上只识别"框架开销是否值得消除",具体消除手段（内联、预提取、直调底层 API 等）是 Phase 3 的工作。

### 三层分析对象

**1. 模型结构层**:模型由哪些组件构成、怎么组织
- 注意力类型(MHA/GQA/MQA/cross-attention)、FFN 结构、Norm 选择
- 重复结构(N 层循环、共享权重)、生成模式(autoregressive/diffusion/encoder-decoder)
- 这一层回答"模型是什么"

**2. 实现逻辑层**:具体代码怎么实现这些组件
- 数据流路径:tensor 从输入到输出经过哪些变换,有没有绕远路
- 生命周期管理:哪些中间结果被创建后立即丢弃、哪些跨步复用
- 控制流:哪些分支在推理时永远走/永远不走、哪些条件判断是多余的
- 数据访问模式:循环内的数据访问是否比必要的更复杂？如用 `gather`/`scatter_` 按动态索引访问,而索引实际在循环前已知；切片产生不必要的 size-1 维度导致下游需要额外的维度变换。这些都是"访问模式可以简化"的信号。
- 这一层回答"代码怎么做的,有没有浪费"

**3. 算法层**:当前的计算方法是否有更优的等价实现
- 同一功能是否存在计算复杂度更低的算法
- 是否有硬件更友好的等价表达(不改功能但改实现路径)
- 等价重组机会:对计算链逐操作追问——这段计算的数学表达能否用结合律/分配律等价重组,使得总 op 数减少或中间 tensor 消除？连续的 element-wise ops 是否构成已知的可融合 pattern？循环内的计算是否可以提前到循环外、或循环外的计算延后到循环内？
- 这一层回答"有没有更好的做法"

> 以上三层均用于**发现问题**,不展开具体变换方案。等价变换是否成立、数据布局怎么重组,是 Phase 3 的职责。

### 四维度驱动提问

通读源码后,对每个计算逻辑块用四维度主动提问。

> **边界**:Phase 2 的四维度用于**发现问题**(问"有没有机会"),不展开"怎么改"。具体实施手段(如何合并、如何预分配、用哪个融合算子)是 Phase 3 的工作,由 03_optimization 的各 reference 承载。这里只需识别出"这里存在去重/复用/掩盖/替换的机会"并量化值不值得做。

**去重**:这段计算有没有多余的工作?
- 有没有重复调用(同一输入的多次独立计算可合并)?
- 有没有死逻辑(推理时不产生效果的分支)?
- 有没有不必要的数据变换(绕了一圈又变回来)?

**复用**:这段计算的结果/资源有没有被浪费?
- 有没有每步都重新计算但结果不变的东西?
- 有没有每步都分配释放同一块内存?
- 有没有跨步/跨层可以共享的中间结果?

**掩盖**:这段延迟有没有机会藏起来?
- 有没有串行但互不依赖的计算(可并行/重叠)?
- 有没有可以和通信重叠的计算段?

**替换**:这段计算有没有硬件更友好的等价写法?
- 有没有更高效的算法(如标准 attention → FlashAttention)?
- 这组计算是否由多个小算子组成、可能有更高效的等价表达?
- 当前 API 在 NPU 上是否有更优的等价表达?

### Profiling 量化(筛选候选)

Line A 发现的候选点需要 profiling 数据量化"值不值得做":
- 识别到某组 Linear 共享同一输入 → 查 op_statistic:这些 Linear 分别占多少时间? 理论上合并后能省多少?
- 识别到某结果每步重算 → 查 operator_memory/op_statistic:重算占总时间多少?
- 如果占比极低(如 <1%)→ 不值得追,跳过

### 热路径操作审计表

对推理热路径（每步/每 token 执行的代码）中的**每个操作**，填写四维度审计表。这是四维度提问的结构化形式——强制对每个操作逐一思考，不能只对"显眼"的操作提问而跳过其余。

| 操作 | 去重：有多余工作? | 复用：输出随输入变? | 输出 shape 固定? | 替换：有多步可实现一步的计算? |
|------|-----------------|-------------------|----------|-----------------|
| _______ | _______ | _______ | _______ | _______ |
| _______ | _______ | _______ | _______ | _______ |

**分析方法**：
- "复用"列：判断操作的输出是否只依赖模型参数或序列形状（不依赖输入内容）。若是，标记"否(不随输入变)"
- "输出 shape 固定?"列：判断操作的输出 shape 在热路径中是否固定。若是，标记"是"。结合 `parse_operator_memory.py` 数据确认是否存在重复同尺寸分配
- "替换"列：检查该操作的算子序列是否构成已知的可融合 pattern（多步操作实现了一步可完成的功能）
- "去重"列：检查是否存在同一输入的多次独立计算、推理时永不走的分支、绕一圈又变回来的数据变换

> 此表是**分析工具**——识别"哪里存在机会"。表中标记为"是"或"否(不随输入变)"的条目意味着该维度有优化空间，但是否值得做、怎么做由后续 profiling 量化和 Phase 3 决定。

### 产出

Line A 的产出是一份"源码问题候选清单",每条含:
- 识别到的问题(如"Q/K/V 三个 Linear 共享同一输入但独立调用")
- 问题位置(源码文件 + 函数/行号)
- profiling 量化的影响范围(如"这三个 Linear 共占 device time 8%")

> 不在此阶段给出解决方案——"怎么改"是 Phase 3(03_optimization)的职责。

---
